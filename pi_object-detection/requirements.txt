fastapi
uvicorn[standard]
picamera2
Pillow
jinja2
tflite-runtime
numpy<2.0
# OpenCV: install via system package (sudo apt install python3-opencv)
# Do NOT use opencv-python-headless here — it requires numpy>=2 which
# conflicts with picamera2's simplejpeg on Raspberry Pi OS.
